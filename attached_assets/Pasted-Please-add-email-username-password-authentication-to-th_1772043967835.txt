Please add email/username + password authentication to this app with the following constraints and features:

Portable REST API, not Replit‑only auth

Implement authentication as a standard REST API so I can call it from a web front‑end now and from a Flutter mobile app later.

Do not tightly couple the login to Replit‑specific front‑end widgets or flows. If you use Replit Auth anywhere, wrap it behind my own endpoints as described below so the Flutter app can call them like any other API.

Endpoints to implement

POST /auth/signup – Accepts JSON with email and password (and optionally name). Creates a user, hashes the password securely, and returns a JSON response with a user object and both an accessToken and refreshToken.

POST /auth/login – Accepts JSON with email and password, verifies credentials, and returns a user object plus accessToken and refreshToken.

POST /auth/refresh – Accepts a valid refreshToken and returns a new accessToken.

POST /auth/logout – Invalidates the refresh token (and any server‑side session if used).

A GET /me endpoint – Returns the current authenticated user based on the access token.

Tokens and security

Use JWT access tokens with a short expiry and refresh tokens with a longer expiry.

Store passwords using a secure hashing algorithm (e.g., bcrypt/argon2) and never store plain‑text passwords.

Implement middleware to protect any routes that should require login, using the Authorization: Bearer <accessToken> header.

Data model and storage

Add a users table/collection to the existing database used by this project (or create one if needed) with fields at least: id, email, passwordHash, createdAt, updatedAt.

Make sure email is unique and return a clear error response if someone tries to sign up with an existing email.

Front‑end integration in this Repl

Add a simple login and signup UI to the existing app (reusing the current styling as much as possible).

Make the front‑end call the REST endpoints listed above using JSON; do not hide the logic behind Replit‑specific client helpers that won’t exist in a Flutter mobile client.

On successful login, store the access token in memory and, if appropriate, refresh tokens in a secure cookie or similar mechanism that is safe for a web app.

Show how to call a protected route and handle “not authenticated” or “token expired” errors gracefully on the front‑end.

Code organization and documentation

Keep the authentication code clearly separated (e.g., in auth routes/controllers and user model files) so I can later point my Flutter app at the same endpoints.

Add short comments at the top of each auth‑related file explaining the purpose of the file and listing the available endpoints.

At the end, print a short checklist in the console describing:

How to create a user,

How to log in (including an example curl or HTTP request),

How to call a protected route from another client like a Flutter app.

Do not break existing features

Keep all current app functionality working. If any existing route needs to be protected, ask me which ones or leave them public with a TODO comment.

Please show me in the code where to plug in my own environment variables (JWT secret, database URL, etc.), and ensure everything runs correctly in this Repl environment.

