name: E2E (Playwright)

# Full localhost E2E is intentionally NOT run on every PR commit (too slow).
# It runs on UAT CD (deploy-uat.yml → uat-e2e-full) and can be triggered manually.
on:
  workflow_dispatch:

jobs:
  e2e:
    uses: ./.github/workflows/_reusable-e2e-local.yml
