# BDD Test Support

This directory contains shared helpers, hooks, and world definitions
used by the step definitions.

## Planned files

- `world.dart` — Custom World class holding test state (users, orgs, pets, tokens)
- `hooks.dart` — Before/After hooks for test setup and teardown
- `api_client.dart` — HTTP helper for calling the API in integration tests
