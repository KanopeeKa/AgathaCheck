/**
 * Pets routes were split into routes/pets/ (see routes/pets/README.md).
 * Monolith backup: routes/pets.js.bak
 *
 * To regenerate modules after editing the backup, use git history for
 * scripts/split-pets-routes.mjs from the split commit, or split manually
 * following the organizations/ pattern.
 */
console.log('Pets routes split complete — see routes/pets/README.md');
