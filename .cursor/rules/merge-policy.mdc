---
description: Branching, merge, and conflict-avoidance policy
alwaysApply: true
---

# Merge policy

- `main` is the release trunk; keep it green.
- **Single-agent / single-domain PRs** → `main` when CI green.
- **Multi-agent sprints** → `cursor/sprint-<N>-<topic>-integration-13e3`; agents merge there; **one PR** to `main`. Use `/spawn-sprint-agents`.

## Before every push

1. `git fetch origin main && git rebase origin/main` (or onto integration parent)
2. `./scripts/pre-push-changed.sh` during work; `./scripts/pre-push.sh` before final merge PR

## Branch naming

- Integration: `cursor/sprint-<N>-<topic>-integration-13e3`
- Agent: `cursor/<descriptive-name>-13e3` or `cursor/<descriptive-name>-feec`

Full ownership matrix and anti-patterns: `.cursor/rules/agent-coordination.mdc` and `docs/agent-efficiency-plan.md`.
